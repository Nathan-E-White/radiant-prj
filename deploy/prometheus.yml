global:
  scrape_interval: 15s # Collect telemetry every 15 seconds

scrape_configs:
  - job_name: 'slurm-security-gateway'
    static_configs:
      - targets: ['api-gateway:8080'] # Points to the Go container within Docker bridge network
